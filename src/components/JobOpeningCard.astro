---
import Button from "./Button.astro";
const { title, location, type, description, applyLink } = Astro.props;
---

<article
  class="flex flex-col gap-2 p-4 bg-gray-100 rounded-lg text-sm shadow-md border border-warm-gold/30"
>
  <small class="text-gray-600">{type} • {location}</small>
  <h3 class="font-bold text-earthy-brown">{title}</h3>
  <p class="text-gray-500 pb-4">
    {description}
  </p>

  <Button href={applyLink}>Apply Now</Button>
</article>
