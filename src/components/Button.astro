---
const { href, align = "self-end", width = "fit-content" } = Astro.props;
---

<a
  href={href}
  class={`${align} ${width} py-2 px-4 bg-warm-gold rounded-lg text-gray-50 font-medium text-sm hover:bg-warm-gold/90 hover:scale-110  active:scale-95 transition-transform outline-none focus:ring-2 focus:ring-warm-gold/50 text-center`}
>
  <button class="cursor-pointer"><slot /></button>
</a>
